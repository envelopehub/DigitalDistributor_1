<Page x:Class="DigitalDistributor.Views.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:DigitalDistributor.Views"
      xmlns:vm="clr-namespace:DigitalDistributor.ViewModels"
      Title="CatalogPage">

    <Page.DataContext>
        <vm:CatalogViewModel/>
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Популярний контент" Style="{StaticResource HeaderText}"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">

            <ItemsControl ItemsSource="{Binding ContentList}">

                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="220" Height="280" Background="White" CornerRadius="10" Margin="0,0,20,20" Padding="15">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
                            </Border.Effect>
                            <StackPanel>
                                <Border Height="130" Background="#BDC3C7" CornerRadius="5" Margin="0,0,0,15"/>

                                <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="18" TextWrapping="Wrap"/>
                                <TextBlock Text="{Binding Genre}" Foreground="Gray" FontSize="12" Margin="0,5,0,5"/>
                                <TextBlock Text="{Binding Price, StringFormat={}{0:C}}" FontWeight="SemiBold" Foreground="#2980B9" Margin="0,0,0,10"/>

                                <Button Content="Детальніше" Height="35" Click="BtnItem_Click"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>

            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>